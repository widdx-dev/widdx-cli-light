# WIDDX Nexus — حالة المشروع

> آخر تحديث: 2026-09-24 · الإصدار: 3.2.0 · Python 3.10+

هذا المستند يصف **الوضع الحالي المتحقق منه**، وليس الادعاءات. كل رقم فيه ناتج عن تشغيل فعلي.

---

## 1. نتائج التحقق

| الفحص | الأمر | النتيجة |
| :--- | :--- | :--- |
| Lint | `python -m ruff check core/ cli/ tui/ scripts/ tests/` | **0 problems** |
| Types | `python -m mypy core/ --ignore-missing-imports --check-untyped-defs` | **0 errors** (222 files) |
| Tests | `python -m pytest tests/ -q` | **873 passed, 1 skipped** (~8 min) |
| Compile | `python -m compileall core/ cli/ tui/ scripts/ tests/` | **clean** |

الاختبار المتخطى (`tests/test_tui.py::test_settings_screen_mounts`) معلّق صراحةً لأن `SettingsScreen` يقرأ التكوين العام ويتعارض مع حالة بقية الاختبارات.

---

## 2. ما يعمل فعلياً

| المجال | التفاصيل |
| :--- | :--- |
| واجهات | CLI (`widdx`)، TUI基于 Textual (`widdx-tui`)، Web (`widdx-web`)، REST API (`widdx-api`) |
| الأدوات | 37 أداة عبر 23 وحدة handler، مع sandbox والتحقق من المسارات |
| التحكم | `ExecutionControlPlane` — قرار واحد مع 5 أفعال و9 طوابير أولوية |
| المراقبة | كشف الانحراف الدلالي، إصلاح ذاتي باللقطة/التراجع، إعادة anchoring |
| التحقق | مدققات AST + وقت تشغيل لـ Game/Web/API/CLI قبل إعلان الانتهاء |
| التدقيق | محرك replay مع checksum + أمر `/replay` |
| الأمان | تشفير Fernet للمفاتيح، `shell=False` على POSIX، حماية المسارات |

---

## 3. حدود معروفة

هذه نقاط صريحة لا يجب إخفاؤها:

1. **الطبقات الإشرافية (MCL / CTI)** تسجّل الحالة وتعرض تقارير، لكنها **لا تغيّر قرارات التنفيذ**. موثّقة في README بعمود "Wired".

2. **مستوى الاستقلالية الذاتي = 3.5** (تقدير داخلي بمقياس `core/runtime/autonomy_metric.py`)، وليس 5.0. النظام أداة هندسة بطبقات تحكم، لا وكيل مستقل متعدد الجلسات.

3. **OpenCode Zen مجاني لكن محدود الحصة.** عند رفض الطلب ينتقل `ReliableProvider` تلقائياً إلى مزود آخر. للاستخدام المستمر يلزم مفتاح مدفوع.

4. **معدل الأخطاء량 مرتفع في بعض الوحدات.** `core/` يحتوي ~490 استدعاء `except Exception`. معظمها يسجّل تحذيراً أو خطأ (لا يبتلع صامتاً)، لكن هذا يبقى مخاطرة قابلية صيانة.

5. **Singleton عبر `global` بلا أقفال** في ~100 موضع. آمن في الاستخدام أحادي الخيط؛ الاستخدام المتزامن يحتاج مراجعة.

6. **توافق الإصدارات**: الاختبارات جرت على 3.12. تُدّعى 3.10–3.13 في `pyproject.toml` وي Infra matrix يختبرها، لكن لم يُتحقق محلياً من كل إصدار.

---

## 4. متطلبات الإنتاج

قبل النشر:

- [ ] **إبطال أي مفتاح API قديم** عبر لوحة المزود (لا علاقة له بالكود)
- [ ] ضبط `WIDDX_API_KEY` بمفتاح قوي في البيئة (لا في الملفات)
- [ ] `pip install -e ".[dev,api]"` ثم تشغيل الفحوص الأربعة أعلاه
- [ ] مراجعة `config.production.json` (يستخدم `deepseek` بمفتاح) مقابل `config.json` (افتراضي مجاني)

---

## 5. خريطة الدخول السريعة

| المهمة | الأداة |
| :--- | :--- |
| تبديل المزود | `/provider` |
| تخزين مفتاح | `/apikey` |
| فحص البيئة | `/doctor` |
| فحص الجلسات المسجلة | `/replay` |
| إعادة تشغيل جلسة | `/replay <run_id>` |
| التحقق من سلامة تسجيل | `/replay <run_id> --verify` |
